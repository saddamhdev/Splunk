## ✅ **FINAL: Splunk installation using Docker (Ubuntu / WSL / Linux on Windows)**

This is the **clean, correct, working guide** based on the exact errors you faced.
Follow **step by step**, no guessing.

Using **Splunk** official Docker image.

---

# 🔧 Prerequisites

* Docker installed & running
* Internet access
* Browser (Chrome / Edge)
* Port **3060** free

Check Docker:

```bash
docker --version
```

---

# 🟢 STEP 1: Pull official Splunk image

```bash
docker pull splunk/splunk:latest
```

---

# 🟢 STEP 2: Remove any failed Splunk container (important)

```bash
docker rm -f splunk
```

*(ignore error if it says container not found)*

---

# 🟢 STEP 3: Run Splunk (CORRECT command)

> ✔ Accepts **license + Splunk General Terms**
> ✔ Uses **port 3060**
> ✔ Uses **strong password**

```bash
docker run -d \
  --name splunk \
  -p 3060:8000 \
  -p 8088:8088 \
  -e SPLUNK_GENERAL_TERMS=--accept-sgt-current-at-splunk-com \
  -e SPLUNK_START_ARGS=--accept-license \
  -e SPLUNK_PASSWORD=Saddam@nvn123 \
  splunk/splunk:latest
```

---

# 🟢 STEP 4: Verify container is running

```bash
docker ps
```

You **must** see:

```
splunk   splunk/splunk   Up
```

If not, check logs:

```bash
docker logs splunk
```

---

# 🟢 STEP 5: Open Splunk Web UI

Open browser:

```
http://localhost:3060
```

Login:

```
Username: admin
Password: Saddam@nvn123
```

🎉 **Splunk is now installed and running successfully**

---

# 🟢 STEP 6: Basic health check (optional)

```bash
docker logs splunk --tail=20
```

You should see:

```
Splunkd started
```

---

# 🧠 Important notes (READ ONCE)

### Password rules (Splunk-enforced)

✔ Uppercase
✔ Lowercase
✔ Number
✔ Special character
✔ 8+ characters

### Ports used

| Port | Purpose                                  |
| ---- | ---------------------------------------- |
| 3060 | Splunk Web UI                            |
| 8088 | HTTP Event Collector (Spring Boot later) |

---

# 🧪 Common Docker commands (cheat-sheet)

```bash
docker stop splunk
docker start splunk
docker restart splunk
docker logs splunk
docker rm -f splunk
```

---

# ✅ Installation checklist

* [x] Docker image pulled
* [x] License accepted
* [x] General terms accepted
* [x] Strong password set
* [x] UI accessible on port 3060

---

## 🔜 NEXT STEPS (tell me the number)

1️⃣ Connect **Spring Boot logs → Splunk (Docker)**
2️⃣ Enable **HTTP Event Collector (best practice)**
3️⃣ Persist Splunk data with Docker volumes
4️⃣ Create dashboards & alerts
5️⃣ Full **Spring Boot + Splunk architecture** (interview-ready)

You’re now **100% set up**. Just say what’s next 👍
